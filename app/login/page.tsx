import Link from "next/link";import {Nav} from "@/components/nav";import {signIn} from "@/actions/auth";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string,message?:string}>}){const p=await searchParams;return <><Nav/><main className="shell"><form action={signIn} className="glass form"><div className="kicker">Welcome back</div><h1>Sign in to MIST.</h1>{(p.error||p.message)&&<p className="muted">{p.error||p.message}</p>}<label className="field">Email<input required type="email" name="email" autoComplete="email"/></label><label className="field">Password<input required type="password" name="password" autoComplete="current-password"/></label><button className="btn btn-primary" type="submit">Sign in</button><p className="muted">New to MIST? <Link href="/register">Create an account.</Link></p></form></main></>}
