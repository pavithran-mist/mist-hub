import Link from "next/link";import {Nav} from "@/components/nav";import {signUp} from "@/actions/auth";
export default async function Register({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <><Nav/><main className="shell"><form action={signUp} className="glass form"><div className="kicker">Your next chapter</div><h1>Join MIST Hub.</h1>{p.error&&<p className="muted">{p.error}</p>}<label className="field">Full name<input required name="name"/></label><label className="field">Email<input required type="email" name="email"/></label><label className="field">Password<input required minLength={8} type="password" name="password"/></label><button className="btn btn-primary" type="submit">Create account</button><p className="muted">Already a member? <Link href="/login">Sign in.</Link></p></form></main></>}
